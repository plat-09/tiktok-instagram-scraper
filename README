# TikTok & Instagram(soon) Scraper

Python-скрипт для сбора статистики с TikTok и Instagram Reels с экспортом в Google Sheets.

## ⚙️ Возможности

- Сбор статистики TikTok (просмотры, лайки, комментарии) через неофициальный API с помощью Playwright.
- Поддержка коротких TikTok ссылок с автоматическим редиректом.
- Интеграция с Google Sheets API для записи данных.
- Частичная поддержка Instagram Reels (в разработке).

## 🚀 Установка

1. Клонируйте репозиторий:
   ```bash
   git clone https://github.com/plat-09/tiktok-instagram-scraper.git
   cd tiktok-instagram-scraper
    

2. Установите зависимости:
    ```bash
    python -m venv venv
    source venv/bin/activate  # или .\venv\Scripts\activate на Windows
    pip install -r requirements.txt
    
3. Установите Playwright:
   ```bash
   playwright install
   # или только Chromium:
   playwright install chromium
    

4. Настройте переменные окружения:
   Создайте файл .env на основе .env.example:

5. Подготовьте файлы creds.json и auth.json на основе creds.example.json и auth.example.json.

📦 Запуск

python scraper.py


⚠️ Важно
	•	Никогда не размещайте реальные creds.json, auth.json или .env в публичных репозиториях.
	•	Instagram Reels поддержка в процессе разработки.
